const connection = require('../config/database');

const Todo = {
    create: (unitData) => {
        
    },
    delete: (id, deletedAt) => {
        
    },
    getAll: (callback) => {
        
    },
    getId: (id) => {
      
    },
    update: function(unitData) {
      
    },
};

module.exports = Todo;