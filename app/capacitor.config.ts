import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'consiss.juanfranco.todoconsissjuanfranco',
  appName: 'todo consiss Juan Franco',
  webDir: 'www'
};

export default config;
